CREATE TABLE scores (
                        id INTEGER PRIMARY KEY AUTOINCREMENT,
                        rank INTEGER NOT NULL,
                        player TEXT NOT NULL,
                        score INTEGER NOT NULL
);